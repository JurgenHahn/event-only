events#show page
<p>
  <%= link_to 'All Events', location_events_path(@location) %>
</p>
<p>
  <%= link_to 'Edit', edit_location_event_path(@location) %>
</p>

<p>
  <%= link_to 'Delete', location_event_path(@location), method: :delete, data: {confirm: "Are you sure you want to delete the event '#{@event.name}'? This cannot be undone!"} %>
</p>



  <div class="event">
    <h3>Name: <%= @event.name %></h3>
    <h4>Date:<%= @event.date %> , Capacity<%= @event.capacity %></h4><br>
  </div>
